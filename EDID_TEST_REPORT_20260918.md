# set_edid 新设备部署与复测报告

日期：2026-09-18  
设备：`pi@192.168.20.19`（SmartLink，AArch64）  
源码：`/home/pi/set-edid-rk3588/s_edid.c`  
程序：`/home/pi/set-edid-rk3588/set_edid`，兼容入口 `/home/pi/set_edid`  
HDMI RX：`/dev/video0`

## 结果摘要

板端 GCC 10.2 使用下述命令编译通过：

```bash
gcc -std=c11 -O2 -Wall -Wextra -Werror -o set_edid s_edid.c
```

最终版本测试 25 个模式：**20 PASS，2 FALLBACK，3 GENERATE_FAIL**。
所有成功生成并写入的模式，设备读回 EDID 与生成数组一致。
测试脚本结束时将测试前的 256 字节 EDID 逐字节恢复，恢复后设备重新锁定
3840×2160@约 60 fps。`PASS` 表示 HDMI RX 连续两次查询锁定目标宽高和帧率，
不表示已单独验证应用层采集画面。

| 结果 | 请求模式 | 实际结果 |
| --- | --- | --- |
| PASS | 640x480@60 | 640x480@59.99 |
| PASS | 1920x1080@60 | 1920x1080@59.99 |
| PASS | 2560x1080@60/75 | 2560x1080@59.97/74.90 |
| PASS | 2560x1440@30/60/75/120/144 | 2560x1440@29.94/59.95/74.96/119.93/143.91 |
| PASS | 3440x1440@60/75/100 | 3440x1440@59.97/74.93/99.93 |
| PASS | 3840x2160@24/25/30/50/60 | 3840x2160@24.00/25.00/30.00/50.00/59.93 |
| PASS | 4096x2160@24/50/60 | 4096x2160@24.00/50.00/59.93 |
| FALLBACK | 4096x2160@25/30 | 1920x1080@约 60 |
| GENERATE_FAIL | 2560x1440@165 | 当前时钟预算下无法生成 |
| GENERATE_FAIL | 3440x1440@120/144 | 当前时钟预算下无法生成 |

## 本次修复和验证依据

1. 非 UHD 模式复制板卡原生 EDID 的身份、版本、显示参数和色度字段。
   受控单变量测试使之前回退或无锁的 640x480@60、2560x1080@60、
   2560x1440@60/75/120 成功锁定；原本通过的 1080p60、3440x1440@60
   未回归。正式版本复测再次通过。
2. 自定义时序像素时钟超过 600 MHz 时，尝试 80 像素紧凑水平消隐；
   `2560x1440@144` 从 604 MHz 降至约 586 MHz，正式版本锁定约 143.91 fps。
   仍超过 600 MHz 的模式继续拒绝生成。
3. `4096x2160@24` 的 HDMI VSDB 添加 HDMI VIC 4，并保留 CTA VIC 98。
   `edid-decode` 识别为 HDMI VIC 4，正式版本锁定 4096x2160@24.00。
4. 正式版本生成的 QHD144、DCI 4K24 EDID SHA-256 与成功的受控实验候选
   完全相同；两个候选的 `edid-decode` 均未输出 `FAIL` 或 `WARN`。

## 未解决与边界

- `4096x2160@25/30`：写入及读回均正确，但当前 HDMI 信号源选择 1080p。
  4096 活动宽度不能编码为 EDID 1.x 基础 DTD；本次只为 @24 加入经过
  验证的 HDMI VIC 4，@25/30 仍只通过 CTA VIC 宣告。是否可通过 DisplayID
  或其他协商方式改善，尚未验证。
- `2560x1440@165`、`3440x1440@120/144`：当前单链路 600 MHz TMDS
  预算下，既有 CVT-RB 及本次紧凑消隐策略均不能满足要求；未扩展到
  DisplayID、DSC 或其他传输方式。
- 本次测试只覆盖这一台接收板与当前连接的 HDMI 信号源。换源、线缆、
  色彩格式或固件后需要重新测试。

## 复现与原始数据

```bash
cd /home/pi/set-edid-rk3588
python3 test_on_device.py
```

每个模式写入后等待 12 秒，再间隔 3 秒查询两次实际输入时序。
原始 CSV 与逐模式 JSON 位于：

```text
/home/pi/set-edid-rk3588/test-results-20260918-141305/
```

Windows 本地副本：`output/test-results-20260918-141305/`。

校验值：

```text
原 EDID SHA-256  483755715c488acb3fb8f8988a9a41a235f41f2df88bbf4a826b5db5a6a9830c
源码 SHA-256     d2363f896915e504f04f85bc8d38b980bd525575d6219a19d88a0e24451ea59f
程序 SHA-256     7a68ea0db746f1a88da2ca35879f8a91ae6105bb20cc103ceef917d150bbb762
```
